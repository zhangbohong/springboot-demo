########################################################
###datasource的type属性可以具体指定到我们需要的数据源上面，
###不指定情况下默认为：org.apache.tomcat.jdbc.pool.DataSource
########################################################
spring:
  master:
    #type: com.zaxxer.hikari.HikariDataSource
    url : jdbc:mysql://localhost:3306/springbootbase?useSSL=false
    username : root
    password : root123
    driver-class-name : com.mysql.jdbc.Driver
  customDatasourceNames:
    names : readonly
  readonly:
    url : jdbc:mysql://localhost:3306/rhcms?useSSL=false
    username : root
    password : root123
    driver-class-name : com.mysql.jdbc.Driver
    #type: com.zaxxer.hikari.HikariDataSource
